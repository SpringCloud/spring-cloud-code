server:
  port: 8082
spring:
  application:
    name: consul-provider
  cloud:
    consul:
      host: 127.0.0.1    # consul 启动地址
      port: 8500         # consul 启动端口
      discovery:
        tags: tag2          # 指定服务的标签, 用逗号隔开
